SafeSeat Main Hub - CLEAN NO-PIEZO runtime
===========================================

Open:
  SafeSeat_Runtime_Main.ino

This folder contains the current Main Hub runtime and no active Piezo code.
Do not merge stale Piezo .cpp/.h files into this sketch folder because Arduino
compiles every source file in the sketch folder.

2026-08-23 MLX note:
  MLX90614 native external-data IF+OCSVM is now active in this Main Hub.
  See MLX_NATIVE_FINAL_RUNTIME_README.txt.
